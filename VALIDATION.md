# PaperScope v6.10 验证清单

## 自动化门禁

- `npm test`
  - 文献读取状态、活跃/归档/回收站统计和筛选语义一致性
  - v3 → v4 迁移、分类树循环修复和批量生命周期操作
  - DOI、arXiv ID、标准化标题的保守匹配
  - PDF 选择回归测试
  - PDF 缩放、笔记字号和笔记图片宽度的空值、非法值与上下限
  - 资讯主题聚类、官方来源标签清理、PDF 页面文本分段和翻译缓存容量控制
  - 导航分区、截图浮窗、阅读器工具位置、分段对照、精简后的更多菜单和 PWA 静态契约
- `npm run build:fixture`
  - 使用确定性离线数据生成完整 `dist`
  - 校验新增 ES 模块和 PWA 资源被复制
  - 校验生成的 `manifest.json` 可解析
- `npm run build`
  - 使用真实外部数据源生成生产构建

GitHub Pages 工作流会在生产构建和部署前运行自动化测试与离线构建。

## 浏览器验收

- 桌面端：导航分区无滚动溢出；设置与更多固定在底部；各分区有轻量视觉区分；设置中心保持两列。
- 移动端（390 × 844）：资讯、设置、文献行和批量工具栏无页面级横向溢出，底部导航和触屏操作可见。
- “更多”菜单：字体与侧栏一致；不再重复设置或无功能帮助；APP 安装入口可用；方向键、Home/End、Esc、点击外部均正确关闭并同步 `aria-expanded`。
- 运行时：桌面和移动端流程完成后浏览器控制台无 error/warn。
- PDF 导入：导入确定性三页 PDF，确认元数据和 DOI 提取、三页文本索引、刷新后持久化。
- PDF 阅读：确认 1/3 页打开、连续滚动文本可见、桌面工具进入侧栏；智能分段、原始文本和分段对照可切换。
- PDF 缩放：首次打开默认 140%；滑杆与数字输入同步；输入 175% 后重新渲染并持久化；越界值会限制在 50%–400%。
- PDF 工具：快速反复点击“更多工具”不产生文字选区；功能名称与 `H/U/N/R/S/Esc/⌘Z` 按键提示保持分离。
- PDF 标注：删除后不出现浏览器确认框，列表和页面标记立即更新；Toast“撤销”可恢复原顺序。两条重合高亮共享单一 25% 透明合成层。
- PDF 截图：按 `S` 或点击截图工具后拖出区域；浮窗内容与原区域一致，可移动、放大、缩小和关闭；关闭阅读器后仍可对照，刷新不持久化。
- 阅读笔记：12–22px 字号即时生效；图片/最近截图插入已保存光标位置且不删除选中文字；单图可在 35%–100% 缩放，刷新后尺寸保持；删除图片可撤销。
- 标题交互：论文列表、首页推荐和文献库标题悬停时均显示下划线，标题文字保持手形指针。
- 论文简介：打开原文入口在侧栏顶部，无需滚动到底部。
- UI 持久化：阅读器工具位置、页面文本模式、侧栏宽度与原有四套预设共同保存；恢复经典方案正常。

## 数据安全重点

- 浏览历史和文献库成员分离；仅浏览条目不会混入“全部文献”或统计。
- 移至回收站支持撤销；恢复不会丢失分类、笔记或附件索引。
- 永久删除会清理文献记录、PDF 文档、逐页文本、批注和关联导入任务，并解除生词关联。
- 旧翻译历史键会在启动时删除；译文缓存最多 300 条且受约 1 MB 字符预算限制，截图不写入持久存储。
- 分类导入会移除无效父级并打断父子循环；UI 阻止同级重名。
- 旧版单对象 PDF 数据库升级时保留旧 store，并迁移到分层 store。
- 写入以 IndexedDB transaction 完成事件为成功边界。
- PDF 替换不删除标注；重复项合并不覆盖已有附件。
- 普通 JSON 导入采用合并策略，不清空现有浏览器文献库。
- 完整备份与附件检查分别覆盖可迁移性和数据一致性。
