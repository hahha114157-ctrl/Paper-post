# PaperScope

## v6.11 笔记历史、Word 导出与独立资讯展开

- 阅读笔记新增独立撤销/重做历史，文字编辑、格式调整、图片插入、图片缩放和图片删除都进入同一历史栈；`Ctrl/Cmd+Z` 可直接撤销图片操作。
- 选中文字或移动光标时，加粗、斜体、下划线、标题、引用和列表按钮会同步显示当前格式状态。
- PDF 阅读器不再响应背景点击或 `Esc` 退出；只有右上角关闭按钮会关闭阅读界面，避免误触丢失阅读位置。
- 笔记导出改为真实 `.docx` 文件，保留标题、段落格式、列表和图片，并在文档开头写入作者、会议/期刊、日期、DOI、原文链接、本地文件名、阅读进度及标注数量。
- 资讯卡片采用两列独立流布局，一侧展开不会再拉动另一侧；展开区根据是否存在原文图片自动调整高度，并增加大图、中文速览和阅读线索。

## v6.10 PDF 阅读标注与图文笔记完善

- PDF 缩放改为 50%–300% 滑杆与 50%–400% 数字输入，支持任意安全比例并在本机记忆。
- 阅读快捷键改为功能名与独立按键胶囊；“更多工具”及按钮文字禁止选中，快速连点不再出现整组文字高亮。
- PDF 标注删除改为立即删除并提供 5 秒撤销，不再弹出浏览器确认框。
- 高亮改用统一透明合成层，同色或不同标注重合时不再逐层累积透明度遮盖原文。
- 阅读笔记新增加粗、斜体、下划线、标题、引用、有序/无序列表、清除格式与 12–22px 字号。
- 本地图片和 PDF 截图严格按已保存的编辑光标插入；非折叠选区不再被图片替换。每张图片支持 35%–100% 独立缩放，删除图片同样可以撤销。

## v6.9 可调阅读侧栏、精确检索与自由图文笔记

- PDF 阅读侧栏可用鼠标拖动、方向键调整宽度，双击恢复当前界面方案的默认值；宽度会保存在本机。
- 阅读配置、搜索结果、标注工具和笔记编辑器彻底解耦；搜索结果和“更多工具”改为浮动下拉层，不再挤占笔记空间。
- 全文搜索支持规范化空白、大小写和全角字符，列出全部匹配、上下跳转，并在 PDF 原页面文本层精确高亮当前结果。
- 阅读笔记升级为按光标位置编辑的图文文档，可在任意两段文字之间插入本机图片或截图，支持标题、加粗、列表、自动保存与原顺序导出。
- 旧版文字与底部图片笔记会自动迁移；图片数量和约 8 MB 容量上限继续生效。
- 资讯卡片只展开当前悬停/聚焦项；展开区改为来源、日期、分组和阅读线索，不再重复摘要。

## v6.8 紧凑阅读工作区与图文笔记

- PDF 阅读工具收进左侧窄栏，阅读画布获得更多横向与纵向空间；OCR、重解析及导入导出归入“更多工具”。
- `S` 启用自由框选截图，截图可悬浮对照，也可压缩后插入当前论文的阅读笔记。
- “页面文本”替换为可输入文字、插入本机图片/截图、自动保存和导出 HTML 的图文阅读笔记；每篇限制 12 张、约 8 MB。
- 主侧栏只突出分区标题，简化图标并把自动更新状态放到最底部。
- 资讯卡片使用构建期数据与 CSS 懒展开补充信息，新增 NVIDIA、AWS 和 GitHub 官方 AI 资讯源。

## v6.7 截图对照与侧栏精简

- PDF 阅读器新增 `S` 截图工具：在页面上拖出区域后生成临时截图浮窗，可拖动、缩放、同时保留最多 4 张，并始终浮在阅读页面之上。
- 截图只存在当前页面内存中，关闭浮窗或刷新后即释放，不写入文献库、`localStorage` 或 IndexedDB。
- 左侧导航各分区使用轻量背景区分；同步状态移到系统区上方，“设置”和“更多”固定在底部并与研究入口隔开。
- “更多”移除重复的设置、翻译历史和无独立功能的帮助入口，APP 安装统一迁入此处；移动端仍保留被底栏折叠的资讯与会议入口。
- 翻译查询历史已删除；可复用译文缓存限制为最多 300 条且约 1 MB，启动时会自动清理旧历史数据。
- 论文原文入口移到简介侧栏顶部；所有可点击论文标题在悬停时显示下划线并使用手形指针。

## v6.6 导航、阅读器与资讯界面升级

- 左侧导航改为“工作台、研究内容、资料与动态、系统”四个分区，折叠与移动端布局继续保留。
- 统一调整字体、行高、卡片间距、筛选控件和辅助文字字号，减少过小文本与无意义的视觉拥挤。
- PDF 阅读器改为清晰的页面导航、搜索、标注和导入/导出层级；阅读模式、缩放、解析与 OCR 可按设置放在顶部或阅读侧栏。
- 页面文本支持智能分段、原始连续文本和分段对照翻译；可逐段翻译或按页生成译文，结果仍只保存在本机。
- 资讯支持按主题聚类、时间排序和来源归纳；相近内容自动靠近，来源、官方标记、日期、标题和摘要不再粘连。
- 经典绿、紧凑学术、专注阅读和高可访问四套原有界面方案完整保留，并增加阅读器工具位置、文本排版和侧栏宽度设置。

## v6.5 文献库与交互升级

- 全部论文标题加入悬停下划线、键盘焦点和行级反馈；文献库选中行增加持续高亮。
- 文献库批量工具改为选择后出现，支持已读/未读、队列、分类、归档、回收站、恢复、引用导出和永久删除。
- 新增待整理、归档和回收站视图；移至回收站后可撤销，永久删除会同步清理 IndexedDB 中的 PDF、文本和批注。
- 分类支持多归属、父子层级、颜色、说明、同级重名校验和循环关系修复；PDF 导入时可直接指定分类。
- 浏览论文不再自动混入“全部文献”，仅保留在最近阅读；收藏、队列、笔记或 PDF 等明确动作才加入文献库。
- 外观、翻译、PDF 导入、备份、存储维护和个人资料集中到侧边栏“设置”页面，原有 UI 配置保持不变。
- 移动端“更多”提供最近阅读、队列、生词、PDF 导入、备份和 APP 安装，并支持 Esc、方向键、焦点恢复和点击外部关闭。

## v6.4 工作流升级

- 统一 PDF 导入中心支持多文件、拖放、文件夹扫描、失败重试、任务取消与刷新后恢复。
- 导入前检查文件签名、150 MB 上限和浏览器剩余空间；使用文件指纹跳过重复项。
- DOI、arXiv ID 或标准化标题完全匹配时可安全关联现有条目，不做模糊自动合并。
- PDF 原文件、逐页文本、标注和导入任务在 IndexedDB 中分层保存；旧版数据首次启动时原位迁移。
- 替换 PDF 时保留标注；合并重复论文时保留多个附件版本；“附件检查”可修复断链并恢复孤立附件。
- “完整备份”在支持 File System Access API 的浏览器中导出文献库、PDF、标注和清单。
- 新增经典绿、紧凑学术、专注阅读和高可访问四套界面方案，并保留原有主题设置。
- 移动端保留 PDF 导入和维护操作，改用下拉筛选与“更多”菜单，避免窄屏表格挤压。

回归验证命令：

```powershell
npm test
npm run build:fixture
```

`build:fixture` 使用确定性的离线数据验证完整静态构建，不依赖外部论文或 RSS 接口；生产构建仍使用 `npm run build`。

PaperScope 是一个面向 AI 与计算机体系结构研究人员的质量优先论文工作台。生产环境使用 **GitHub Actions 定时抓取数据 + GitHub Pages 静态发布**，不需要 Render 或常驻服务器。

固定网址：<https://hahha114157-ctrl.github.io/Paper-post/>

完整操作见 [GitHub Pages 部署与 APP 安装指南](DEPLOYMENT_GUIDE.md)。

## 个人文献库

- 个人主页与研究方向资料。
- 收藏、阅读队列、阅读进度、已读历史、笔记、自定义标签与摘要文字标注。
- 自定义专题收藏、批量操作以及 2～3 篇论文并排对比。
- 基于 arXiv 元数据及 Crossref 标题匹配的正式发表状态跟踪。
- JSON 导入/导出备份，以及 BibTeX / Markdown 引用导出。
- 混合阅读翻译：离线多义词典、本地段落翻译和可选在线精译，可将结果加入论文笔记或生词本。
- 本地 PDF 工作区：可以给目录论文附加 PDF，也可以直接把任意 PDF 作为本地论文加入文献库。
- PDF 文字恢复：优先解析原生文本层；扫描件或异常编码页可按页使用中英文浏览器 OCR，并将结果加入全文检索与翻译。
- PDF 阅读标注：翻译、高亮、下划线和文字批注共用按视觉行重建的选区；已修正 PDF.js 6 缩放变量、异字号双栏行归组和拖选期间的工具栏位移，支持窄栏缝识别与分栏边界裁切，不再串到上一段、相邻栏或漏掉行末文字。
- 论文与团队追溯：结合本地作者交集、Crossref 和 Semantic Scholar 查看公开机构、作者指标、共同作者与先前工作。
- Zotero 风格文献整理：分层专题、一个条目加入多个专题、智能检索、未归档、重复项合并、附件全文检索与可编辑元数据。

个人数据只保存在当前浏览器。结构化记录位于 `localStorage`，PDF 原文件和逐页文本位于 `IndexedDB`，不会上传到 GitHub。JSON 备份不包含 PDF 二进制文件；清理浏览器数据或更换设备前请分别导出重要 PDF。

## 站内 PDF 阅读

- 论文详情中的“导入 PDF”会解析本地文件、提取逐页文本，并将原文件保存在当前浏览器的 IndexedDB。
- “站内阅读”提供分页、缩放、全文搜索、页面渲染和可翻译文本；桌面端可把阅读配置放入侧栏，移动端自动回到顶部。
- 页面文本可切换智能分段、原始文本或分段对照翻译；对照模式支持逐段翻译和按页生成，并复用本机翻译缓存。
- 阅读器使用接近全屏的工作区，可在“上下连续滚动”和“单页翻页”之间切换。可以先拖选再按高亮/下划线/批注，也可以先启用工具再拖选并自动保存；标注模式不会同时触发翻译。
- 截图工具使用 `S` 启用区域框选；截图浮窗可拖动、通过按钮或右下角调整大小，最多保留 4 张临时对照图。
- 标注快捷键：`H` 高亮、`U` 下划线、`N` 文字批注、`R` 区域、`Shift+N` 区域批注、`S` 截图、`Esc` 取消、`Ctrl/Cmd+Z` 撤销；`PageUp/PageDown` 可切换当前页。
- 标注和 PDF 原文件分开保存在 IndexedDB，避免每次标注都重写整个文件。可以随时导出带可见标注和中文标注摘要的 PDF 副本，原文件保持不变。
- “导出标注数据”生成可重新导入的 JSON。普通文献库 JSON 备份仍不包含 PDF 和 PDF 标注，请对重要附件单独备份。
- 文献库顶部的“本地 PDF”可以导入目录中不存在的论文，标题和作者优先读取 PDF 元数据。
- 单文件上限为 150 MB。扫描版 PDF 若没有文本层，可以点击“OCR 本页”或“OCR 无文本页”；OCR 在浏览器本机执行，首次使用需要下载 `eng` 和 `chi_sim` 语言数据。
- OCR 结果取决于页面清晰度、语言和版式。旋转页面的标注会写入导出摘要，但当前不会扁平绘制到原页面，以避免坐标错误。
- 网站不会自动镜像受版权保护的全文。开放版本仅提供可核验入口；跨站 PDF 还可能受浏览器 CORS 限制，因此手动下载后导入最稳定。

## Zotero 风格文献管理

- 专题收藏支持父子层级；同一论文可同时加入多个专题，专题更接近播放列表而不是磁盘文件夹。
- “智能检索”动态筛选未读、有 PDF、有 PDF 标注、最近加入和元数据待补充的条目，并支持检索已解析/OCR 的附件全文。
- “未归档”显示尚未加入任何专题的收藏或附件；“重复项”按 DOI 优先、标准化标题兜底识别，可合并笔记、标签、专题、阅读状态和附件。
- 论文详情中的“编辑元数据”可修正标题、作者、日期、会议/期刊、DOI 和原文网址，适合给独立 PDF 建立父条目。

## 论文与团队追溯

- 有 DOI 时使用 Crossref 核验论文作者机构，并优先通过 ORCID 精确检索先前工作；Semantic Scholar 用于补充作者档案、共同作者和历史论文。
- 没有外部匹配或接口限流时，使用当前 PaperScope 数据中的作者交集给出本地追溯结果。
- “机构”是论文或作者档案公开记录，不代表作者当前所在机构；“团队线索”基于共同作者重合，不会自动声称某个实验室或课题组。
- 追溯结果会保存到当前浏览器，可手动刷新。外部计数和机构信息仍应点击来源页面复核。

## 阅读翻译

- 顶部“译”按钮可以总开关阅读翻译，并分别控制单词/术语点击、选区自动翻译、引擎策略和本地缓存。
- 翻译仅在论文摘要、详情和文献库阅读文本中生效，不会抢占标题链接、按钮或输入框。
- 单词优先查询 60,000 条 ECDICT 通用词库及 PaperScope AI/体系结构专业术语表，显示多词性、多义项、音标、英文解释和词形。
- 通用词典按首字母分片加载，也可以在设置中一次性下载完整离线词典；数据来源和许可见 [第三方数据声明](THIRD_PARTY_NOTICES.md)。
- 段落优先使用浏览器内置 `Translator API`；首次使用可能需要确认下载语言包。
- 翻译框可拖动；“固定位置”会记住坐标，“跟随选区”会自动显示在文字附近，移动端使用底部卡片。
- 在线精译通过用户自行部署的 Cloudflare Worker 调用 DeepL 或 LibreTranslate。第三方 API 密钥只保存在 Worker Secret，不写入网页，详见 [在线精译代理说明](worker/README.md)。
- 生词本支持查询次数、掌握度、朗读和 CSV 导出；翻译只保留受容量限制的可复用缓存，不保存查询历史；论文详情支持生成中英对照摘要。
- PWA 静态脚本使用版本化地址；发现新版后会提示一键刷新，刷新不会清除收藏、笔记、生词本或其他本地资料。

## 页面与导航

站点使用适合 GitHub Pages 的 Hash 路由，不需要服务器重写规则：

- `#/home`：精简研究概览。
- `#/ai`、`#/architecture`：独立论文列表、会议/期刊筛选、质量排序与分页。
- `#/curated`：按会议或期刊当届专栏归类的正式收录论文、今日必读与未来两周计划。
- `#/library/*`：收藏、队列、最近阅读、笔记、发表动态、专题和生词本。
- `#/news`、`#/venues`：分页资讯与会议期刊目录。
- `#/paper/<id>`：可复制和返回的论文详情链接。

支持浏览器前进/后退、深色模式、`Ctrl/Cmd + K` 快速跳转和移动端底部导航。

## 数据来源

- arXiv API：AI 栏目使用 `cs.AI`、`cs.LG`、`cs.CL`、`cs.CV`、`cs.RO`；体系结构栏目使用 `cs.AR`、`cs.DC`、`cs.PF`。
- PMLR、NeurIPS Proceedings、ACL Anthology：ICML、NeurIPS、ACL 正式论文集与权威元数据。
- Crossref REST API 与出版方 DOI：CVPR、TPAMI、ISCA、MICRO、HPCA、ASPLOS、IEEE TC、ACM TACO 等会议和期刊专栏。
- Semantic Scholar Academic Graph：论文匹配、作者档案、共同作者和历史工作追溯；公共接口限流时自动回退到本地结果。
- arXiv 回退：正式出版元数据没有机器可读摘要时，按标准化标题匹配对应 arXiv 版本，并保留正式出版链接。
- 官方 RSS：OpenAI、Google DeepMind、Microsoft Research。
- 会议与期刊：官方 CFP / 作者指南链接、人工核验日期和动态截止状态。

## 质量优先推荐与每日阅读

- 默认排序依次考虑旗舰会议/期刊正式收录、官方来源、Crossref 被引信号、重点主题相关性和年份；最近几天上传只占很弱的权重。
- 首页每天推荐一篇值得精读的论文，提供推荐理由、正式收录时间、45 分钟分段阅读计划与三个复盘问题。
- 北京时间零点后切换当日论文；GitHub Actions 每天 06:30 抓取并发布最新数据。
- “完成今日阅读”会记录在当前浏览器，可在首页或顶会精选页查看打卡状态。

构建失败时不会发布空数据，也不会生成假论文；GitHub Pages 会继续保留上一次成功部署。

## 自动更新

工作流位于 `.github/workflows/pages.yml`：

- 每天北京时间 06:30 自动构建和发布。
- 推送到 `main` 分支时自动发布。
- 可以在 GitHub 仓库的 **Actions** 页面手动运行。

GitHub Actions 使用 UTC，工作流中的 `30 22 * * *` 对应次日北京时间 06:30。

## 本地构建

需要 Node.js 18 或更新版本：

```powershell
npm run build
npm start
```

打开 <http://localhost:4173>。`npm run build` 需要访问外部论文和 RSS 数据源。

可选配置：

```powershell
$env:CONTACT_EMAIL="your-email@example.com"
npm run build
```

`CONTACT_EMAIL` 用于 Crossref polite pool，不要提交真实邮箱到仓库。

## 目录

```text
.github/workflows/pages.yml  自动抓取和 Pages 部署
scripts/build-static.mjs     聚合论文、资讯、热点和会议数据
scripts/preview.mjs          本地静态预览服务
index.html / app.js          前端页面和交互
pdfjs-dist                   本地 PDF 页面渲染与文本提取
pdf-lib                      带标注 PDF 副本生成
tesseract.js                 浏览器本地 OCR
manifest.webmanifest         PWA 安装配置
service-worker.js            静态资源与数据离线缓存
dist/                        构建产物，不提交到 Git
```

## 数据边界

- “热点”是当前批次关键词统计，不代表完整领域的全局趋势。
- “推荐分”是可解释的站内排序信号，不等同于学术界公认排名；会议/期刊正式收录和来源可信度高于新鲜度。
- “结构化摘要”只重组来源摘要，不虚构实验结果，也不是大模型翻译。
- 会议信息是官方页面的核验快照，临时变更仍应以链接页面为准。
- 当前没有后台 Web Push；关闭 APP 后主动收到通知需要独立的推送服务。
