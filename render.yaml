services:
  - type: web
    name: paperscope
    runtime: node
    plan: free
    buildCommand: echo No build required
    startCommand: node server.mjs
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
